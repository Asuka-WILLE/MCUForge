# 轮毂电机平滑控制实机问题记录（2026-07-16）

## 1. 记录目的

本文件记录从表现较好的 `pc_test_v2` 基线继续增加万向轮归正、启动补偿、停车残速保护和单轮失速保护后，实车出现的主要问题与遥测证据。

当前结论：后续版本引入的状态机、补偿和驱动器复位过多，实机操控明显不如 v2。本轮调试停止继续叠加补偿，源码和固件回退到 `flash_pc_test_v2.log` 对应版本。

## 2. 回退基线

- 构建记录：`MDK-ARM/build_pc_test_v2.log`
- 烧录记录：`MDK-ARM/flash_pc_test_v2.log`
- 构建时间：2026-07-16 14:26:11
- 烧录完成时间：2026-07-16 14:26:34
- 当时程序大小：`Code=75330`，`RO-data=15258`，`RW-data=268`，`ZI-data=10444`
- 编译结果：`0 Error(s), 0 Warning(s)`
- v2 的主要内容：线速度和转向 S 曲线、受限 USB 实机测试；尚未加入后续复杂的万向轮归正、直线同步修正、频繁双驱复位和单轮失速重试链。

## 3. 已观察到的实车问题

### 3.1 零速启动方向不稳定

- 启动瞬间曾出现轻微右偏。
- 第二段加速度时曾出现明显右偏。
- 加入固定补偿后又出现补偿过度、向左偏。
- 多次启动的偏转方向和幅度不完全一致：
  - 约五次零速启动累计左偏约 10°；
  - 后续版本变成每次明显右偏；
  - 某次第四次启动突然左偏约 10°，第五、六次又稍微回正。

结论：偏差不是一个稳定的固定左右增益误差，继续使用固定偏置会在左右偏之间来回过补偿。

### 3.2 停车末段偏转和残余旋转

- 快停止时出现大幅右偏，接近右转。
- 松开转向后，车辆偶发继续低速旋转 1～2 s。
- 后退归零后，曾出现右轮停止、左轮继续运动 1～2 s，方向偏移约 45°。
- 多次停车遥测触发双驱速度环复位，说明停车后残余轮速问题频繁存在，而不是单次偶发。

### 3.3 后退时随机单轮运行

该问题不能稳定复现，但在长时间遥控测试中多次出现：

- 按后退时左轮不动、右轮运动，车辆原地转圈。
- 另一次按后退时右轮不动、左轮运动。
- 曾出现按后退后车辆原地转约一圈。

关键点：失效侧会变化，因此不能简单判定为固定某一侧机械阻力或只给一侧增加补偿。

### 3.4 万向轮归正状态机影响正常操控

- 后续版本加入 `BRAKE -> CRAWL -> RUN/FAILED` 后，启动前等待时间明显增加。
- `CRAWL` 低速阶段本身会产生可见偏转。
- 多次进入 `FAILED` 或执行闭环复位，使正常遥控被打断。
- 状态机为了避免单轮运行而频繁停机、复位、重新归正，整体驾驶体验远差于 v2。

### 3.5 左右转松手偶发延迟

- 左转或右转时，偶发松开方向后仍继续低速旋转 1～2 s。
- 遥控器帧丢失、`rc_ready` 重建条件、停车轨迹和驱动器残余目标都可能参与该问题。
- 当前还没有完成单一根因确认。

## 4. 代表性遥测证据

### 4.1 正常速度命令下单轮失速

2026-07-16 17:01:55.357～17:01:58.367：

- 目标线速度：`-25 RPM`
- 左右命令：约 `-25 / +25 RPM`
- 左轮反馈：约 `-26 RPM`
- 右轮反馈：`0 RPM`
- 持续时间：约 3.0 s

说明软件已经给出方向正确、幅值对称的后退命令，但一侧驱动没有形成实际轮速。

2026-07-16 16:59:06.723～16:59:10.376：

- 目标线速度：`-25 RPM`
- 左右命令：约 `-26 / +24 RPM`
- 左轮反馈：约 `-3～-4 RPM`
- 右轮反馈：约 `+24～+29 RPM`
- 持续时间：约 3.65 s

该次低速/失速侧与上一例不同，进一步证明问题不是固定单侧偏置。

### 4.2 CRAWL 阶段一轮已动、另一轮未动

2026-07-16 17:20:45.970～17:20:46.501：

- 目标直行：`+25 RPM`
- CRAWL 命令逐步修正到约 `+10 / -6 RPM`
- 左轮反馈持续为 `0 RPM`
- 右轮反馈约为 `-3 RPM`
- 约 600 ms 后执行双驱速度环复位

说明动态补偿已经提高未动侧命令并压低先动侧命令，但仍无法立即拉起未动轮；保护触发前车辆已经产生可见偏转。

### 4.3 正常运行失速保护触发

2026-07-16 17:12:02.575：

- 目标线速度：`-25 RPM`
- 反馈约为 `-23 / +4 RPM`
- `align_run_reset_count` 从 0 增加到 1

说明即使通过启动归正阶段，正常速度爬升后仍可能出现一侧掉速。

## 5. 已尝试但效果不理想的措施

- 线速度和转向 S 曲线，限制加速度、减速度和 jerk。
- 启动时固定左右轮偏置。
- 两段式启动偏置。
- 根据实际反馈动态判断先动侧并进行强/弱补偿。
- 最大 1 RPM 的直线交叉同步修正。
- 每次停车后重新执行万向轮归正。
- 停车残余轮速检查与双驱速度环复位。
- CRAWL 单轮未启动重试。
- RUN 阶段单轮失速重试和失败锁零。
- 尝试增加 Modbus 写回显确认；该方案尚未完成实车验证，而且所在版本整体体验已经明显劣于 v2。

## 6. 当前判断

1. 当前问题不是单纯“左轮快”或“右轮快”，固定补偿无法稳定解决。
2. 后续版本把机械静摩擦、万向轮换向、遥控输入、驱动器执行和停车残速混在一个复杂状态机中，保护互相叠加，正常操控被频繁打断。
3. 单轮不动既可能来自机械起步阻力，也可能来自驱动器速度环、使能状态或 RS485 写事务；现有遥测还不足以完全区分。
4. 应先回到 v2 的简单、可控基线，再一次只增加一个可验证功能。

## 7. 后续重新排查建议

回到 v2 后，下一轮建议按以下顺序进行，并且每一步单独形成可回退版本：

1. 只保留 S 曲线，确认前进、后退、转向和停车基本操控。
2. 增加驱动器写命令诊断，但先只记录应答和失败计数，不改变运动行为。
3. 确认单轮不动时是“命令未写入”还是“命令写入但电机未形成速度”。
4. 如果是机械起步差异，只在检测到一轮已经运动、另一轮未运动时短时限制先动轮，不做固定方向补偿。
5. 停车残速和启动不同步分开处理，不再共用频繁禁用/使能驱动器的复位链。

